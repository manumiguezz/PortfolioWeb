typedef HeroRevealListenerHandle = Object;

bool isHeroRevealStarted() => true;

HeroRevealListenerHandle? addHeroRevealListener(void Function() onReveal) {
  return null;
}

void removeHeroRevealListener(HeroRevealListenerHandle? handle) {}
